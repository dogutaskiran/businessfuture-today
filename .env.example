# PubMesh subscription/contact endpoint. Leave empty until the capability is exposed.
PUBMESH_SUBSCRIBE_URL=
PUBMESH_SERVICE_TOKEN=

# Optional webhook secret for future PubMesh content revalidation.
PUBMESH_REVALIDATE_SECRET=
