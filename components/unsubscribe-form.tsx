"use client";import {useState} from "react";export function UnsubscribeForm({token}:{token:string}){const[state,setState]=useState<"idle"|"loading"|"done"|"error">("idle");async function go(){setState("loading");const r=await fetch("/api/newsletter/unsubscribe",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token})});setState(r.ok?"done":"error")}if(state==="done")return <p>You’ve been unsubscribed. You won’t receive future briefings unless you subscribe again.</p>;return <div><button className="legal-primary-button" disabled={state==="loading"} onClick={go}>{state==="loading"?"Unsubscribing…":"Unsubscribe"}</button>{state==="error"?<p>We couldn’t process this link. Email <a href="mailto:privacy@businessfuture.today">privacy@businessfuture.today</a>.</p>:null}</div>}
