import {PublicationFooter,PublicationHeader} from "@/components/publication/chrome";import {canonicalTemplate} from "@/lib/publication";export function LegalShell({title,eyebrow="LEGAL",updated="September 2, 2026",children}:{title:string;eyebrow?:string;updated?:string;children:React.ReactNode}){const template=canonicalTemplate;return <div className="publication publication--newsroom"><PublicationHeader template={template}/><main className="pub-shell legal-page"><p className="pub-kicker">{eyebrow}</p><h1>{title}</h1><p className="legal-updated">Last updated {updated}</p><div className="legal-content">{children}</div></main><PublicationFooter template={template}/></div>}
