#!/bin/bash
set -euo pipefail
. /etc/businessfuture-automation.env
exec curl -fsS --retry 3 --retry-all-errors --retry-delay 10 --connect-timeout 15 --max-time 330 -H "x-bft-scheduler-token: ${BFT_SCHEDULER_TOKEN}" -H 'accept: application/json' https://businessfuture.today/api/automation/run
